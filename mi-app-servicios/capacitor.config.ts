import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.carlos.miappservicios',
  appName: 'mi-app-servicios',
  webDir: 'build'
};

export default config;
